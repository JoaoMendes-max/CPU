# Rotation Release Notes — R\_\_ (YYYY-MM-DD to YYYY-MM-DD)

## TL;DR (3 bullets)

- Did x, y, and z, which will enable a and b for our users.
- Fixed issues a, b, and c, which were causing d and e.
- Started work on feature x, which will enable y and z for our users.

## What shipped (merged PRs)

### Pipeline

We completed x, y, and z in the pipeline, which will enable a and b for our users.
For more details, see the following PRs:

- PR #\_\_\_ —
- PR #\_\_\_ —

### Compiler

We completed x, y, and z in the compiler, which will enable a and b for our users.
For more details, see the following PRs:

- PR #\_\_\_ —
- PR #\_\_\_ —

### Peripherals

We completed x, y, and z in the peripherals, which will enable a and b for our users.
For more details, see the following PRs:

- PR #\_\_\_ —
- PR #\_\_\_ —

## Behavior changes (must)

- None / (describe change + why)

## Tests / Verification performed / added

- Testbench/sim used for verification of feature x (link PRs/issues with `#`).
- Testbench/sim used for verification of feature y (link PRs/issues with `#`).

## Known issues (carry to next rotation)

Open issues are:

- Issue #\_\_\_ — (why still open, what blocks it)

## Risks / Callouts for next rotation (⚠)

Example - be careful with:

- Timing of x, y, and z (link issues/PRs with `#`).
- Additional testing needed for feature x (link issues/PRs with `#`).

## Suggested focus areas for next rotation

The suggested next steps are:

1. Fix issues a, b, and c (link issues with `#`).
2. Complete work on feature x (link PRs/issues with `#`).
3. Start work on feature y (link PRs/issues with `#`).
